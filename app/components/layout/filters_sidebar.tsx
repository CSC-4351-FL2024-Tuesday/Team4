import * as React from 'react';

export interface FiltersSidebarProps {
}

export default class FiltersSidebar extends React.Component<FiltersSidebarProps> {
  public render() {
    return (
      <div className='w-full h-screen'>
        Filters Sidebar
      </div>
    );
  }
}
